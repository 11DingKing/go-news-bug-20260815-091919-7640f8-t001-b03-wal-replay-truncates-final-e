## 题目元信息

- 来源：`self_built`
- 处理流程：`Codex Gold 修复`
- Bug 分类：`slice`
- 复现稳定性：`stable`
- 难度：`advanced`
- 目标 Bug 数量：`1`
- 上游 Issue：无（0-1 自建项目）

# BUG_TASK

来源：self_built

## 题目
WAL 回放按换行切分时多做一次截断，丢失文件末尾最后一个事件

## Bug 分类
- 类别：slice
- 症状类型：wrong_result（错误结果）
- 难度：advanced

## 用户可见症状
服务崩溃重启后回放 WAL 时，文件末尾最后一个事件（该事件写入时未以换行结尾）被丢弃，导致该事件对应的作业记录缺失，回放后索引中的事件总数比实际写入总数少 1。该现象容易被误判为「崩溃瞬间正在写入的事件本就未完成持久化」，但该事件其实已完整落盘，只是回放时被错误地丢弃。

## 正确行为
回放必须完整恢复 WAL 中所有已持久化事件，无论最后一行是否以换行结尾。

## 稳定复现命令
go test -run TestReplayRecoversEveryPersistedEvent -count=1 ./internal/reconcile/

## 验收标准
- 正常路径：以换行结尾的 WAL 可完整回放全部事件。
- 失败或并发路径：最后一条事件无换行结尾时，回放仍恢复全部事件（不丢尾事件）。
- 无回归断言：回放后索引事件总数与写入总数一致，且 seq 顺序正确。
